import { Menu } from '@mui/material';

const FileMenu = ({ anchorRef }) => {
  return (
    <Menu open={false} anchorEl={anchorRef}>
      <div>dwwed</div>
    </Menu>
  );
};

export default FileMenu;
